name: field.blocks.text.name
icon: text
wysiwyg: true
preview: text
fields:
  spacing:
    label: Interlettrage (pour résoudre les problèmes d'orpheline(s))
    type: number
  text:
    label: Texte
    type: writer
    nodes: false
    placeholder: field.blocks.text.placeholder